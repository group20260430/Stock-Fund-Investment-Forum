# 股票基金投资论坛 — 全面测试提示词文档

> 按三大测试维度组织，覆盖项目全部 5 大业务模块、28 张数据库表、40+ API 端点、20+ 前端页面。
> 每个提示词均可直接复制使用，粘贴代码上下文后即可让 AI 生成对应测试。

---

## 目录

1. [单元测试提示词](#1-单元测试提示词)
2. [接口测试提示词](#2-接口测试提示词)
3. [功能测试用例提示词](#3-功能测试用例提示词)

---

## 1. 单元测试提示词

> 每个提示词均包含：测试要求、控制结构分支说明、边界场景、覆盖率要求。
> 使用时将对应服务的完整代码粘贴到提示词中。

---

### 1.1 UserService.register() — 注册方法

```
为以下 UserService 类的 register 方法生成 Pytest 单元测试。

【方法功能】
手机号注册：查重 phone → hash 密码 → 生成昵称 → 映射注册类型枚举 → 创建用户 → 签发 Token → 返回 Profile

【控制结构覆盖要求】
1. if-else 分支：重复手机号检查（existing user → 409 / 正常 → 继续）
2. try-except 分支：RegisterType 枚举映射（合法值 → 正常 / 非法值 → 回退到 PHONE）
3. if-else 分支：昵称自动生成（有 nickname → 用传入值 / 无 nickname → 用"用户+手机尾号"）
4. if 分支：avatar_url 可选处理

【测试用例要求】
- 正常注册：完整请求 → 验证返回 user_id、token、refresh_token、expires_in=7200、user profile
- 重复手机号：同一 phone 注册两次 → 第一次 201，第二次 409 "该手机号已注册"
- 无昵称注册：不传 nickname → 自动生成 "用户xxxx"
- 非法 register_type：传 "invalid" → 回退到 RegisterType.PHONE
- 带 avatar_url 注册：验证 avatar_url 正确存入
- mock password_hash：验证 get_password_hash 被调用且结果存入 user
- mock Token 签发：验证 create_access_token 和 create_refresh_token_record 被调用

【使用 pytest-mock + SQLAlchemy mock session】
- 使用 mocker.patch 模拟 db.query().filter().first() 的返回值
- 使用 mocker.patch 模拟 get_password_hash / create_access_token / create_refresh_token_record
- fixture 提供独立的 mock db session

【代码上下文 - 请将以下内容粘贴到提示词中】
- UserService 完整类代码（register 方法及其依赖的 _issue_token_pair 和 _build_profile）
- RegisterRequest Pydantic schema
- User 模型（含 phone, password_hash, nickname, avatar_url, register_type, auth_level, role, status）
- RegisterType 枚举（PHONE, EMAIL）
- AuthLevel 枚举（BASIC, VERIFIED, REAL_NAME, PROFESSIONAL）
- UserStatus 枚举（ACTIVE, BANNED, DISABLED）
- get_password_hash / create_access_token / create_refresh_token_record 函数签名
```

### 1.2 UserService.login() — 登录方法

```
为以下 UserService 类的 login 方法生成 Pytest 单元测试。

【方法功能】
登录验证：查用户 → 判断登录类型（密码/验证码）→ 校验密码/验证码 → 每日签到积分 → 签发 Token → 返回 Profile

【控制结构覆盖要求】
1. if-else 分支：login_type 判断（"password" → 验证密码 / "code" → 验证验证码）
2. if-else 分支：用户不存在 → 404
3. if-else 分支：用户已禁用（status == BANNED/DISABLED）→ 403
4. if-else 分支：密码校验失败 → 401
5. if-else 分支：验证码校验失败 → 401
6. if 分支：每日首次登录 → 调用 award_points +1 积分
7. if 分支：非首次登录 → 不重复加分

【测试用例要求】
- 密码登录正常：正确手机号+密码 → 200，返回 token + user profile
- 验证码登录正常：正确手机号+验证码 → 200
- 密码错误：错误密码 → 401
- 验证码错误：错误验证码 → 401
- 用户不存在：未注册手机号 → 404
- 用户已封禁：BANNED 状态 → 403
- 每日签到积分：当日首次登录 → 验证 award_points 被调用
- 非首次登录不重复加分：同一天再次登录 → 验证 award_points 不被调用

【使用 pytest-mock】
- mock db session 查询
- mock verify_password / VerificationCodeStore.get
- mock award_points

【代码上下文】
- UserService.login() 完整代码
- LoginRequest schema
- User 模型（含 status 字段）
- verify_password 函数签名
- award_points 函数签名
```

### 1.3 VerificationCodeStore — 验证码存储

```
为以下 VerificationCodeStore 类生成 Pytest 单元测试。

【类功能】
内存验证码存储，基于 dict + TTL 过期清理。

【控制结构覆盖要求】
1. set(): 直接写入 key→{code, expires_at}
2. get(): entry is None → return None / 未过期 → return code / 已过期 → delete + return None
3. delete(): key 存在 → pop / key 不存在 → 静默忽略
4. cleanup_expired(): 遍历 dict → 找到过期条目 → 删除 → 返回删除数量
5. 空 dict 时 cleanup_expired → return 0

【测试用例要求】
- set + get 正常：存入后立即获取 → 返回正确 code
- get 过期：mock time.time()，设置 time.time() > expires_at → return None，条目被删除
- get 不存在 key：返回 None
- delete 存在 key：删除后 get 返回 None
- delete 不存在 key：不抛异常
- cleanup_expired：一个已过期一个未过期 → 删除 1 条，返回 1
- TTL 精确性：set 时 ttl_seconds=300 → expires_at = time.time() + 300

【使用 time mocking】
- mocker.patch('time.time') 控制时间流逝

【代码上下文】
- VerificationCodeStore 完整类代码
```

### 1.4 UserService.send_code() / verify_code() / reset_password()

```
为以下 UserService 的验证码相关方法生成 Pytest 单元测试。

【方法功能】
- send_code(): 根据类型（register/login/reset_password）检查用户状态 → 生成 6 位验证码 → 存入 Store → 开发模式返回 dev_code
- verify_code(): 从 Store 获取 → 比对 → 删除原码 → 设置 verified 标记
- reset_password(): 检查 verified 标记 → 查用户 → 更新密码

【控制结构覆盖要求】
send_code():
1. if-elif-else：type == "register" → 用户不能已存在 / type == "login" → 用户必须存在 / type == "reset_password" → 用户必须存在
2. if-else：用户存在/不存在的不同判断
3. if：settings.smtp_configured 为 False 时返回 dev_code

verify_code():
1. if：stored_code is None → 401
2. if：stored_code != code → 401
3. 验证通过 → delete 原码 → set verified 标记

reset_password():
1. if：verified 标记不存在 → 400 "请先验证验证码"
2. if：用户不存在 → 404
3. 正常 → 更新 password_hash → commit

【代码上下文】
- UserService.send_code() / verify_code() / reset_password() 完整代码
- SendCodeRequest / ResetPasswordRequest schema
- settings.smtp_configured 配置项
```

### 1.5 DuplicateContentService — 重复内容检测

```
为以下 DuplicateContentService 类生成 Pytest 单元测试。

【类功能】
检测用户的新帖子是否与最近的帖子重复：精确匹配 → 拦截，模糊匹配 >= 92% → 审核，其他 → 通过。

【控制结构覆盖要求】
1. _normalize_text():
   - if：None 值处理 → 返回空字符串
   - if：空文本 → 返回空字符串
   - 遍历 char：过滤标点符号（P/S 类别）
   - re.sub 合并空白
2. check_duplicate_post_content():
   - if：归一化文本 < MIN_DUPLICATE_TEXT_LENGTH(20) → 提前返回 "text_too_short"
   - for 循环：遍历 recent_posts（最多 50 条）
   - if：现有文本太短 → 跳过比较
   - if：精确匹配（existing_normalized == normalized_text）→ 立即返回 should_block=True
   - if-elif：模糊匹配（similarity >= 0.92）→ should_review=True / 否则记录 best_match
   - 最终 return "no_duplicate_detected"

【测试用例要求】
- 文本太短（<20字符）→ status="ok", reason="text_too_short"
- 精确匹配 → should_block=True, similarity=1.0, status="exact_duplicate"
- 模糊匹配 0.92~0.99 → should_review=True, status="near_duplicate"
- 模糊匹配 0.91 → 通过（不触发审核）
- 完全无匹配 → 正常通过
- 用户最近无帖子 → 正常通过
- 多个帖子中找到最佳匹配 → 返回相似度最高的
- 标点符号和大小写不影响匹配（NFKC 归一化验证）

【使用 mock db session】
- mock db.query(Post).filter().order_by().limit().all() 的返回值

【代码上下文】
- DuplicateContentService 完整类代码
- Post 模型（含 id, user_id, title, content, created_at）
- DuplicateContentCheckResult dataclass
- 常量：MIN_DUPLICATE_TEXT_LENGTH=20, RECENT_POST_LIMIT=50, NEAR_DUPLICATE_THRESHOLD=0.92
```

### 1.6 PointsService — 积分与等级系统

```
为以下积分和等级系统生成 Pytest 单元测试。

【方法功能】
- get_level(points): 遍历阈值表 → 找到 points 对应的等级
- award_points(db, user_id, points, reason): 查用户 → 加/减积分 → 重算等级 → 写 PointsHistory

【控制结构覆盖要求】
get_level():
1. for 循环 + if-else：遍历 LEVEL_THRESHOLDS，points >= threshold → 升级 / 否则 break
2. 边界：0→L1, 99→L1, 100→L2, 299→L2, 300→L3, 599→L3, 600→L4, 999→L4, 1000→L5, 1999→L5, 2000→L6, 4999→L6, 5000→L7, 9999→L7, 10000→L8

award_points():
1. if：user is None → 提前 return，不操作
2. points += 计算：user.points = (user.points or 0) + points
3. level 重新计算：user.level = get_level(user.points)
4. 创建 PointsHistory 记录

【测试用例要求 - 使用 @pytest.mark.parametrize 批量测试】
- 所有等级边界值：0, 99, 100, 299, 300, 599, 600, 999, 1000, 1999, 2000, 4999, 5000, 9999, 10000
- 所有积分事件类型测试：
  | 事件 | 分值 | 断言 |
  |------|------|------|
  | daily_login | +1 | points 增加 1 |
  | create_post | +5 | points 增加 5 |
  | create_comment | +2 | points 增加 2 |
  | post_liked | +1 | points 增加 1 |
  | comment_liked | +1 | points 增加 1 |
  | post_shared | +3 | points 增加 3 |
  | gained_follower | +1 | points 增加 1 |
  | delete_post | -5 | points 减少 5 |
  | delete_comment | -2 | points 减少 2 |
  | post_unliked | -1 | points 减少 1 |
  | comment_unliked | -1 | points 减少 1 |
  | lost_follower | -1 | points 减少 1 |
- 用户不存在时 award_points 静默忽略
- PointsHistory 记录的 reason / reference_type / reference_id 是否正确传入
- 从 L2 降到 L1（积分扣减后低于 100）

【代码上下文】
- get_level() 和 award_points() 完整代码
- PointsHistory 模型
- User 模型（含 points, level 字段）
- LEVEL_THRESHOLDS = [(100,2), (300,3), (600,4), (1000,5), (2000,6), (5000,7), (10000,8)]
```

### 1.7 SensitiveWordService — 敏感词检测

```
为以下敏感词检测服务生成 Pytest 单元测试。

【方法功能】
检查文本内容是否包含敏感词，按级别返回：BLOCK（拦截）、REVIEW（审核）、WARN（提醒）。

【控制结构覆盖要求】
1. for 循环：遍历所有 is_active=True 的敏感词
2. if-elif-else：level == BLOCK → 立即拦截返回 / level == REVIEW → 标记审核 / level == WARN → 标记警告
3. if：is_active=False → 跳过，不参与检测
4. 遍历完成无命中 → 返回正常

【测试用例要求】
- BLOCK 级别命中 → should_block=True
- REVIEW 级别命中 → should_review=True
- WARN 级别命中 → 仅警告，不拦截
- 多个敏感词同时命中 → 返回最高级别处理
- 停用词（is_active=False）→ 不触发检测
- 中文敏感词匹配：内容包含"禁发词" → 拦截
- 英文敏感词匹配：内容包含"spam" → 拦截
- 无敏感词 → 正常通过
- 空内容 → 正常通过

【代码上下文】
- SensitiveWordService.check_content() 完整代码
- SensitiveWord 模型（word, level: BLOCK/REVIEW/WARN, is_active, category）
- SensitiveLevel 枚举
```

### 1.8 ComplianceService — 合规规则检测

```
为以下合规检测服务生成 Pytest 单元测试。

【方法功能】
使用正则表达式规则检测内容是否违规：荐股规则、市场操纵规则等。

【控制结构覆盖要求】
1. for 循环：遍历所有 is_active=True 的合规规则
2. if：正则 re.search 匹配 → 按规则级别处理
3. if-elif-else：按 level 分类处理（block / review / warn）
4. if：is_active=False → 跳过
5. 遍历完成无匹配 → 返回正常

【测试用例要求】
- "推荐大家买入XXXX股票" → 触发荐股规则
- "我们一起拉升这支票" → 触发市场操纵规则
- "今天天气不错，适合投资" → 不触发规则
- 禁用规则不应生效
- 多个规则同时匹配 → 返回最高级别处理
- 中文正则匹配验证

【代码上下文】
- ComplianceService 完整代码
- ComplianceRule 模型（pattern 正则表达式, category, level, is_active）
- 禁用规则 is_active=False 的过滤逻辑
```

### 1.9 QualityService — 内容质量评分

```
为以下内容质量评分服务生成 Pytest 单元测试。

【方法功能】
对帖子内容进行质量评分：长度、结构、多样性、实质内容四个维度综合打分。

【控制结构覆盖要求】
1. if-else：内容长度分段评分（<50字→低分, 50-200→中分, 200-500→高分, >500→最高分）
2. if-else：结构评分（有标题加分、有段落分隔加分）
3. if-else：多样性评分（重复内容扣分）
4. if-else：实质内容评分（财经/投资关键词密度）
5. 空内容 → 返回基准最低分

【测试用例要求】
- 空内容 → 最低分（验证分数 < 20）
- 只有标题无内容 → 中低分
- 优质长文（>500字，有标题，多段落，含财经关键词）→ 高分（>80）
- 纯重复内容 → 低多样性分
- 含大量财经关键词 → 实质内容高分
- 短文本但结构完整 → 中等分数

【代码上下文】
- QualityService.score_content(title, content) 完整代码
- QualityScore 数据结构
```

### 1.10 MentionService — @提及解析与通知

```
为以下 @提及解析服务生成 Pytest 单元测试。

【方法功能】
解析文本中的 @用户名，找到被提及用户并分发通知。

【控制结构覆盖要求】
parse_mentions():
1. 正则匹配：找出所有 @用户名 模式
2. if：@后面无内容 → 忽略
3. 多个 @提及 → 全部解析
4. 无 @提及 → 返回空列表

dispatch_mentions():
1. for 循环：遍历解析出的用户名
2. if：用户名存在 → 创建通知
3. if：用户名不存在 → 静默忽略
4. @自己 → 不创建通知（防止自通知）

【测试用例要求】
- "@张三 来看看这个" → 解析出 ["张三"]
- "同时@李四 @王五 两个" → 解析出 ["李四", "王五"]
- "@" 单独 → 空列表
- 无 @提及 → 空列表
- @不存在的用户 → 静默忽略
- @自己 → 不创建通知
- 多个 @提及 → 每个被提及用户都收到通知

【代码上下文】
- MentionService 完整代码
- 正则表达式模式
- Notification 模型（type="mention"）
```

### 1.11 UserService.refresh_token() — Token 刷新

```
为以下 Token 刷新逻辑生成 Pytest 单元测试。

【方法功能】
Refresh Token Rotation：验证旧 Token → 吊销旧 Token → 签发新 Token 对。

【控制结构覆盖要求】
1. if：Token 不存在于数据库 → 401
2. if：Token 已被吊销（is_revoked=True）→ 401
3. if：Token 过期 → 401（同时吊销该 Token 族）
4. if：hash 比对失败 → 401
5. 正常 → 标记旧 Token 为 revoked → 创建新 RefreshToken → 签发新 access_token

【测试用例要求】
- 正常刷新 → 返回新 access_token + refresh_token，旧 Token 被吊销
- 使用已吊销 Token 刷新 → 401
- 使用过期 Token 刷新 → 401
- Token 格式错误 → 401
- hash 比对失败（被篡改）→ 401

【代码上下文】
- UserService.refresh_token() 完整代码
- RefreshToken 模型（user_id, token_hash, expires_at, is_revoked, revoked_at）
- hash_refresh_token() 函数
- create_access_token() 函数
```

### 1.12 行情 MarketService — 数据源代理与降级

```
为以下行情数据代理服务生成 Pytest 单元测试。

【方法功能】
获取实时指数行情：主数据源东方财富 → 降级新浪财经 → 最终降级空数据。

【控制结构覆盖要求】
1. try-except：请求东方财富 API
2. if：东方财富成功 → 解析并返回数据
3. except（超时/网络异常）→ 降级到新浪财经
4. try-except：请求新浪财经 API
5. if：新浪成功 → 解析并返回新浪格式数据
6. except → 最终降级，返回空数据标记

【测试用例 - 使用 pytest-httpx 或 respx mock】
- 东方财富正常 → 返回东方财富格式数据
- 东方财富超时 → 自动降级到新浪
- 两个数据源都失败 → 返回空数据标记
- K 线数据不同周期（日/周/月/5分钟）
- 内置指数返回验证：上证(000001), 沪深300(000300), 深证成指(399001), 中证500(000905)

【代码上下文】
- MarketService 完整代码
- 东方财富 API URL 和响应格式
- 新浪降级 API URL 和响应格式
- 降级配置和超时设置
```

---

## 2. 接口测试提示词

> 每个提示词均包含：接口路径、请求方法、请求体/参数格式、正常/异常/边界三类测试用例。
> 使用时可直接将 curl 命令复制到 Postman 或 Swagger Inspector。

---

### 2.1 用户认证 API

```
根据以下 FastAPI 后端接口定义，对用户认证模块进行完整的 API 测试。

【测试工具】Postman / Swagger Inspector / pytest-httpx
【基础 URL】http://localhost:8000/api

---

#### 接口 1: POST /auth/register — 手机号注册

请求体：
{
  "phone": "13800138001",
  "password": "Abc@123456",
  "nickname": "测试用户",
  "avatar_url": null,
  "register_type": "phone"
}

测试用例：
| # | 场景 | 请求体变化 | 状态码 | 预期响应 |
|---|------|-----------|-------|---------|
| 1 | 正常注册 | 完整参数 | 201 | data.user_id, data.token, data.refresh_token |
| 2 | 重复手机号 | 相同 phone | 409 | detail:"该手机号已注册" |
| 3 | 无昵称注册 | 不传 nickname | 201 | data.user.nickname="用户8001" |
| 4 | 密码太短 | password="123" | 422 | 校验错误 |
| 5 | 手机号格式错误 | phone="123" | 422 | 校验错误 |
| 6 | 缺少必填字段 | 只传 phone | 422 | 校验错误 |

---

#### 接口 2: POST /auth/login — 登录

密码登录请求体：
{
  "phone": "13800138001",
  "password": "Abc@123456",
  "login_type": "password"
}

验证码登录请求体：
{
  "phone": "13800138001",
  "code": "123456",
  "login_type": "code"
}

测试用例：
| # | 场景 | 变化 | 状态码 | 预期 |
|---|------|------|-------|------|
| 1 | 密码登录正常 | 正确 phone+password | 200 | data.token, data.user |
| 2 | 验证码登录正常 | 正确 phone+code | 200 | data.token |
| 3 | 密码错误 | 错误 password | 401 | detail:"密码错误" |
| 4 | 验证码错误 | 错误 code | 401 | detail:"验证码错误" |
| 5 | 用户不存在 | 未注册 phone | 404 | detail:"该手机号未注册" |
| 6 | 用户已封禁 | BANNED 用户 | 403 | detail:"账号已被封禁" |

---

#### 接口 3: POST /auth/send-code — 发送验证码

请求体：{"phone":"13800138001","type":"register"}
type 可选值：register / login / reset_password

测试用例：
| # | 场景 | 变化 | 状态码 | 预期 |
|---|------|------|-------|------|
| 1 | 注册发送 | type=register, 手机未注册 | 200 | expire_in=300, dev_code |
| 2 | 注册时已注册 | type=register, 已存在 | 409 | "该手机号已注册" |
| 3 | 登录发送 | type=login, 已注册 | 200 | 成功 |
| 4 | 登录时未注册 | type=login, 不存在 | 404 | "该手机号未注册" |
| 5 | 重置密码发送 | type=reset_password | 200 | 成功 |

---

#### 接口 4: POST /auth/verify-code — 验证验证码

请求体：{"phone":"13800138001","code":"123456","code_type":"register"}

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 验证码正确 | 200, data.verified=true |
| 2 | 验证码错误 | 401, "验证码错误或已过期" |
| 3 | 验证码过期(>5min) | 401 |

---

#### 接口 5: POST /auth/reset-password — 重置密码

请求体：{"phone":"13800138001","code":"123456","password":"NewPw@789"}

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 正常重置 | 200 |
| 2 | 未验证验证码 | 400, "请先验证验证码" |
| 3 | 手机号未注册 | 404 |

---

#### 接口 6: POST /auth/refresh — 刷新 Token

请求体：{"refresh_token":"旧refresh_token字符串"}

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 正常刷新 | 200, 返回新 access+refresh |
| 2 | 已吊销 Token | 401 |
| 3 | 过期 Token(>7天) | 401 |

---

#### 接口 7: GET /auth/me — 获取当前用户

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 有效 Token | 200, data.user |
| 2 | 无 Token | 401 |
| 3 | 伪造 Token | 401 |

---

#### 接口 8: PUT /auth/profile — 更新个人资料

请求体：{"nickname":"新昵称","bio":"新简介","avatar_url":"https://..."}

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 正常更新 | 200, 数据更新 |
| 2 | 仅更新单项 | 200, 其他字段不变 |
| 3 | 超长昵称(>50字) | 422 |

---

#### 接口 9: GET/PUT /auth/privacy — 隐私设置

隐私结构：{"profile_visibility":"public","message_permission":"everyone","show_investment_info":true,"show_follow_lists":true,"show_activity_status":true}

测试用例：
| # | 场景 | 变化 | 预期 |
|---|------|------|------|
| 1 | GET 获取 | 当前设置 | 200 |
| 2 | PUT 仅粉丝可见 | profile_visibility=followers_only | 200 |
| 3 | PUT 私密 | profile_visibility=private | 200 |
| 4 | PUT 关闭消息 | message_permission=none | 200 |
```

### 2.2 内容系统 API

```
对帖子内容系统模块进行完整的 API 测试。

【基础 URL】http://localhost:8000/api

---

#### 接口 1: GET /categories — 板块列表

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 正常获取 | 200, 树形结构，顶级分区含子板块 |
| 2 | 空板块 | 200, data=[] |

---

#### 接口 2: GET /posts — 帖子列表

参数：category_id, user_id, keyword, sort_by(latest/hot/trending), page, page_size

测试用例：
| # | 场景 | 参数 | 预期 |
|---|------|------|------|
| 1 | 全部帖子 | 无参数 | 200, data.total, data.items |
| 2 | 按板块筛选 | category_id=1 | 该板块帖子 |
| 3 | 按关键词搜索 | keyword="股票" | 含关键词帖子 |
| 4 | 按热度排序 | sort_by=hot | 热度降序 |
| 5 | 分页 | page=2, page_size=10 | 第2页 |
| 6 | 空分类 | category_id=999 | data.total=0 |
| 7 | 搜索无结果 | keyword="不存在" | data.total=0 |

---

#### 接口 3: POST /posts — 创建帖子

4 种帖子类型：

【normal】{"category_id":1,"title":"测试帖","content":"内容","post_type":"normal","tags":["测试"]}
【longtext】{"category_id":1,"title":"长文","content":"...","post_type":"longtext","cover_image":"url"}
【poll】{"category_id":1,"title":"投票","content":"怎么看？","post_type":"poll","vote_options":[{"label":"看多"},{"label":"看空"}]}
【realtime】{"category_id":1,"title":"实时","content":"直播中","post_type":"realtime","is_live":true}

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 创建普通帖 | 201, data.id, data.title |
| 2 | 创建长文帖 | 201, data.post_type="longtext" |
| 3 | 创建投票帖 | 201, data.vote_options 有 N 项 |
| 4 | 创建实时帖 | 201, data.is_live=true |
| 5 | 未登录创建 | 401 |
| 6 | 帖子含敏感词 | 拦截/审核 |
| 7 | 投票选项<2项 | 422 |
| 8 | 标题为空 | 422 |

---

#### 接口 4: GET /posts/{id} — 帖子详情

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 正常获取 | 200, data 含内容/附件/投票/标签 |
| 2 | 帖子不存在 | 404 |
| 3 | 含投票帖 | data.vote_options 含结果 |

---

#### 接口 5: PUT /posts/{id} — 编辑帖子

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 作者编辑 | 200, 内容更新 |
| 2 | 非作者编辑 | 403 |
| 3 | 不存在帖子 | 404 |

---

#### 接口 6: DELETE /posts/{id} — 删除帖子

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 作者删除 | 200 |
| 2 | 非作者删除 | 403 |
| 3 | 不存在帖子 | 404 |

---

#### 接口 7: POST /posts/{id}/vote — 投票

请求体：{"option_ids":[1]}

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 正常投票 | 200, data.results |
| 2 | 重复投票 | 409 |
| 3 | 对非投票帖投票 | 400 |
```

### 2.3 交互 API（评论/点赞/收藏/转发）

```
对交互模块进行完整的 API 测试。

---

#### 接口 1: GET /posts/{id}/comments — 评论列表

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 正常获取 | 200, 含一级评论+楼中楼 |
| 2 | 无评论帖子 | 200, data=[] |
| 3 | 帖子不存在 | 404 |

---

#### 接口 2: POST /posts/{id}/comments — 发表评论

一级评论：{"content":"好文章！"}
楼中楼：{"content":"同意","parent_id":1,"reply_to":1}

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 一级评论 | 201 |
| 2 | 楼中楼回复 | 201, data.parent_id=1 |
| 3 | 未登录 | 401 |
| 4 | 评论含敏感词 | 拦截/审核 |
| 5 | 空内容 | 422 |

---

#### 接口 3: DELETE /comments/{id} — 删除评论

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 作者删除 | 200 |
| 2 | 非作者删除 | 403 |
| 3 | 评论不存在 | 404 |

---

#### 接口 4: POST /comments/{id}/like — 评论点赞

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 点赞 | 200, data.liked=true |
| 2 | 取消点赞(再点) | 200, data.liked=false |

---

#### 接口 5: POST /posts/{id}/like — 帖子点赞

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 点赞 | 200, data.liked=true, like_count+1 |
| 2 | 取消点赞 | 200, data.liked=false, like_count-1 |

---

#### 接口 6: POST /posts/{id}/collect — 收藏/取消

请求体：{"folder_id":1}

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 收藏 | 200 |
| 2 | 取消收藏 | 200, 从收藏列表移除 |

---

#### 接口 7: GET /users/me/collections — 我的收藏

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 有收藏 | 200, data 列表 |
| 2 | 无收藏 | 200, data=[] |

---

#### 接口 8: POST /posts/{id}/share — 转发

请求体：{"content":"推荐"}

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 正常转发 | 200 |
| 2 | 未登录 | 401 |
```

### 2.4 社交关系 API

```
对社交关系模块进行完整的 API 测试。

---

#### 接口 1: GET /users/{id} — 用户公开资料

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 公开用户 | 200, 完整资料 |
| 2 | 私密用户(非粉丝) | 200, 部分信息隐藏 |
| 3 | 私密用户(是粉丝) | 200, 完整信息 |
| 4 | 用户不存在 | 404 |

---

#### 接口 2: POST /users/{id}/follow — 关注/取消

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 关注 | 200, data.following=true |
| 2 | 取消关注 | 200, data.following=false |
| 3 | 关注自己 | 403 |
| 4 | 重复关注→取消 | 200 |

---

#### 接口 3-6: 关注/粉丝列表

测试用例：
| # | 接口 | 场景 | 预期 |
|---|------|------|------|
| 1 | GET /me/followers | 有粉丝 | 200, 粉丝列表 |
| 2 | GET /me/followers | 无粉丝 | 200, data=[] |
| 3 | GET /me/following | 有关注 | 200, 关注列表 |
| 4 | GET /me/following | 无关注 | 200, data=[] |
| 5 | GET /{id}/followers | 公开用户 | 200 |
| 6 | GET /{id}/following | 公开用户 | 200 |

---

#### 接口 7: PUT /users/me/starred — 星标用户

请求体：{"user_id":2,"starred":true}

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 设星标 | 200 |
| 2 | 取消星标 | 200 |

---

#### 接口 8: GET /users/{id}/points — 公开积分/等级

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 公开用户 | 200, data.points, data.level |
| 2 | 隐私限制 | 按设置返回 |
```

### 2.5 群组社区 API

```
对群组社区模块进行完整的 API 测试。

---

#### 接口 1: POST /groups — 创建群组

请求体：{"name":"价值投资群","description":"...","is_public":true,"need_approval":false}

测试用例：
| # | 场景 | 状态码 | 预期 |
|---|------|-------|------|
| 1 | 创建公开群 | 201 | data 含群组信息 |
| 2 | 创建私密审批群 | 201 | |
| 3 | 未登录创建 | 401 | |
| 4 | 名称为空 | 422 | |

---

#### 接口 2: GET /groups — 群组列表

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 我的群组 | 200, 已加入的群 |
| 2 | 发现群组 | 200, 公开群组 |

---

#### 接口 3: GET /groups/{id} — 群组详情

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 成员查看 | 200, 含成员列表 |
| 2 | 未加入看公开群 | 200, 基本信息 |
| 3 | 未加入看私密群 | 403 |
| 4 | 群不存在 | 404 |

---

#### 接口 4: POST /groups/{id}/join — 加入群组

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 加入公开群 | 200, 直接加入 |
| 2 | 申请私密群 | 200, status=pending |
| 3 | 重复加入 | 409 |
| 4 | 已退出后重入 | 200 |

---

#### 接口 5: POST /groups/{id}/leave — 退出群组

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 成员退出 | 200, 无法查看群内容 |
| 2 | 群主退出 | 400/需转让 |

---

#### 接口 6: DELETE /groups/{id} — 解散群组

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 群主解散 | 200 |
| 2 | 普通成员 | 403 |

---

#### 接口 7: PUT /groups/{id} — 编辑群组

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 群主编辑 | 200 |
| 2 | 成员编辑 | 403 |

---

#### 接口 8: POST /groups/{id}/posts — 群内发帖

请求体：{"title":"讨论帖","content":"今天怎么看？"}

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 成员发帖 | 201 |
| 2 | 非成员 | 403 |

---

#### 接口 9: GET /groups/{id}/posts — 群内帖子

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 成员查看 | 200 |
| 2 | 非成员 | 403 |

---

#### 接口 10: POST /groups/{id}/members/approve — 审核成员

请求体：{"user_id":5,"action":"approved"}

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 审核通过 | 200, 成员可看群 |
| 2 | 审核拒绝 | 200, 通知申请人 |

---

#### 接口 11: DELETE /groups/{id}/members/{uid} — 移出成员

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 群主移出 | 200 |
| 2 | 移出群主 | 403 |
| 3 | 非群主操作 | 403 |

---

#### 接口 12: POST /messages — 发送消息

私信：{"receiver_id":2,"content":"你好","msg_type":"text"}
群聊：{"group_id":1,"content":"大家好","msg_type":"text"}

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 发送私信 | 201 |
| 2 | 向未关注者发私信 | 403(隐私限制) |
| 3 | 群聊发消息 | 201 |

---

#### 接口 13: GET /messages — 获取消息

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 私信列表 | 200 |
| 2 | 群聊消息 | 200 |
```

### 2.6 发现与搜索 API

```
对发现搜索模块进行完整的 API 测试。

---

#### 接口 1: GET /feed — 个性化 Feed

参数：page, page_size

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 正常获取 | 200, 混合 Feed |
| 2 | 分页 | 第2页不同数据 |

---

#### 接口 2: GET /hot — 热榜

参数：period(day/week/month), market

测试用例：
| # | 场景 | 参数 | 预期 |
|---|------|------|------|
| 1 | 日热榜 | period=day | 200, 热度降序 |
| 2 | 周热榜 | period=week | 不同数据 |
| 3 | 月热榜 | period=month | 不同数据 |
| 4 | 市场筛选 | market=沪深 | 过滤 |
| 5 | 空热榜 | - | 200, data=[] |

---

#### 接口 3: GET /search — 全文搜索

参数：keyword, search_type(post/user/stock/group), page, page_size

测试用例：
| # | 场景 | 参数 | 预期 |
|---|------|------|------|
| 1 | 搜帖子 | keyword="股票", search_type=post | 匹配帖子 |
| 2 | 搜用户 | keyword="张三", search_type=user | 匹配用户 |
| 3 | 搜群组 | keyword="投资", search_type=group | 匹配群组 |
| 4 | 无结果 | keyword="不存在的内容xxx" | data.total=0 |

---

#### 接口 4: GET /recommendations — 个性化推荐

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 有投资偏好 | 匹配偏好内容 |
| 2 | 新用户 | 热门/精华 |

---

#### 接口 5: GET /search/recommendations — 搜索页推荐

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 正常获取 | 200, 热门帖子+用户+股票 |

---

#### 接口 6: GET /search/suggestions — 搜索联想

参数：q

测试用例：
| # | 场景 | q | 预期 |
|---|------|---|------|
| 1 | 股票联想 | "000" | 匹配股票 |
| 2 | 用户联想 | "张" | 匹配用户 |
| 3 | 空输入 | "" | data=[] |
```

### 2.7 行情数据 API

```
对行情数据模块进行完整的 API 测试。

---

#### 接口 1: GET /market/indices — 实时指数行情

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 正常获取 | 200, 指数列表(代码/名称/最新价/涨跌幅) |
| 2 | 数据源降级 | 主源失败→新浪或空数据 |

---

#### 接口 2: GET /market/kline/{secid} — K 线数据

参数：period(daily/weekly/monthly/5min)

测试用例：
| # | 场景 | period | 预期 |
|---|------|--------|------|
| 1 | 日K线 | daily | 200, 含日期/开/高/低/收/量 |
| 2 | 周K线 | weekly | 200 |
| 3 | 月K线 | monthly | 200 |
| 4 | 5分钟K线 | 5min | 200 |
| 5 | 无效secid | - | 404 |
```

### 2.8 通知 API

```
对通知模块进行完整的 API 测试。

---

#### 接口 1: GET /notifications — 通知列表

参数：type, unread_only, page, page_size

测试用例：
| # | 场景 | 参数 | 预期 |
|---|------|------|------|
| 1 | 全部通知 | 无 | 200, data |
| 2 | 按类型筛选 | type=mention | 只返回 @提及 |
| 3 | 仅未读 | unread_only=true | 只返回未读 |
| 4 | 无通知 | - | 200, data=[] |

---

#### 接口 2: PUT /notifications/read — 标记已读

单条：{"notification_ids":[1,2,3]}
全部：{"all":true}

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 标记单条已读 | 200, 变为已读 |
| 2 | 标记全部已读 | 200, 全部已读 |

---

#### 接口 3: GET /notifications/unread-count — 未读数

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 有未读 | 200, data.count>0 |
| 2 | 全部已读 | 200, data.count=0 |
```

### 2.9 管理后台 API

```
对管理运营模块进行完整的 API 测试。管理接口需要管理员角色 Token。

---

#### 接口 1: POST /report — 提交举报

请求体：{"target_type":"post","target_id":1,"reason":"违规内容","description":"..."}

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 正常举报 | 201 |
| 2 | 重复举报 | 409 |
| 3 | 未登录 | 401 |

---

#### 接口 2: GET /admin/review-queue — 审核队列

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 管理员 | 200, 待审核列表 |
| 2 | 非管理员 | 403 |

---

#### 接口 3: POST /admin/review-queue/{id}/review — 执行审核

通过：{"action":"approved","review_comment":"合规"}
拒绝：{"action":"rejected","review_comment":"违规"}

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 审核通过 | 200, 内容发布 |
| 2 | 审核拒绝 | 200, 内容下架, 通知作者 |

---

#### 接口 4: GET /admin/users — 用户列表

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 全部用户 | 200 |
| 2 | 搜索用户 | keyword="张三" → 匹配 |
| 3 | 按状态筛选 | status=banned → 封禁用户 |

---

#### 接口 5: POST /admin/users/{id}/ban — 封禁/解封

封禁：{"action":"ban","reason":"违规","duration_hours":72}
解封：{"action":"unban"}

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 封禁用户 | 200, 用户无法登录 |
| 2 | 解封用户 | 200, 用户恢复 |

---

#### 接口 6: GET /admin/stats/overview — 数据总览

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 正常获取 | 200, DAU/帖子数/用户数/趋势/告警 |

---

#### 接口 7: GET /admin/stats/trend — 趋势数据

参数：period(daily/weekly/monthly)

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 日趋势 | 200 |
| 2 | 周趋势 | 200 |
| 3 | 月趋势 | 200 |

---

#### 接口 8: GET /admin/stats/hot-topics — 热门话题

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 正常获取 | 200, 话题排行+趋势对比 |

---

#### 接口 9: GET /admin/stats/engagement — 参与度报告

测试用例：
| # | 场景 | 预期 |
|---|------|------|
| 1 | 正常获取 | 200, 互动率/活跃度 |
```

---

## 3. 功能测试用例提示词

> 每个提示词均包含：交互场景描述、测试用例表（含编号/场景/前置/步骤/预期/优先级）。
> 可直接复制给 AI 生成完整的功能测试用例。

---

### 3.1 用户注册与登录

```
根据以下交互场景，生成完整的功能测试用例表。

【场景描述】
新用户在论坛首页点击"注册"按钮，进入注册页面，输入手机号，
点击"获取验证码"，收到验证码后输入，设置密码和昵称，提交注册。
系统自动登录并跳转到首页。用户退出后，使用相同账号重新登录。

【用例清单】

| 编号 | 场景 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|------|-------|
| REG-01 | 正常注册(手机号) | 手机未注册 | 输入手机号→获取验证码→输入验证码→设密码→设昵称→提交 | 201注册成功，自动登录跳首页 | P0 |
| REG-02 | 正常注册(邮箱) | 邮箱未注册 | 输入邮箱→收验证码→设密码→提交 | 注册成功，自动登录 | P0 |
| REG-03 | 重复手机号 | 手机已注册 | 输入已注册手机号→获取验证码→提交 | 提示"该手机号已注册" | P0 |
| REG-04 | 重复邮箱 | 邮箱已注册 | 输入已注册邮箱→提交 | 提示"该邮箱已注册" | P0 |
| REG-05 | 验证码过期 | 获取后>5分钟 | 输入过期验证码→提交 | 提示"验证码已过期" | P1 |
| REG-06 | 验证码错误 | 已获取 | 输入错误验证码 | 提示"验证码错误" | P1 |
| REG-07 | 密码强度不足 | - | 输入<8位或纯数字密码 | 前后端校验提示 | P1 |
| REG-08 | 无效手机号 | - | 输入非手机号格式 | 格式校验错误 | P1 |
| REG-09 | 不设昵称 | - | 不填昵称→正常其他 | 自动生成"用户+尾号" | P1 |
| REG-10 | 注册后自动登录 | 注册成功 | 检查首页状态 | 显示已登录用户信息 | P0 |
| REG-11 | 密码登录 | 已注册 | 输入手机号+密码→登录 | 200登录成功，跳首页 | P0 |
| REG-12 | 验证码登录 | 已注册 | 手机号→获取验证码→输入→登录 | 200登录成功 | P1 |
| REG-13 | 密码错误登录 | 已注册 | 输入错误密码 | 401 "密码错误" | P0 |
| REG-14 | 用户不存在登录 | 未注册 | 输入未注册手机号 | 404 "该手机号未注册" | P0 |
| REG-15 | 退出登录 | 已登录 | 点击退出→确认 | 退出成功，Token失效 | P0 |
| REG-16 | Token过期 | 登录>2小时 | 操作需登录接口 | 自动刷新Token或跳登录 | P1 |
| REG-17 | 重置密码 | 已注册 | 忘记密码→输入手机号→收验证码→验证→设新密码→用新密码登录 | 密码重置成功 | P1 |
| REG-18 | 重置密码未验证 | 已注册 | 跳过验证码直接重置 | 400 "请先验证验证码" | P1 |
```

### 3.2 帖子浏览与创建

```
根据以下交互场景，生成完整的功能测试用例表。

【场景描述】
用户在论坛首页浏览板块列表，进入"股票讨论区"查看帖子列表，
翻页浏览，点击一个帖子查看详情，然后创建新帖子（普通帖/投票帖/长文帖）。

【用例清单】

| 编号 | 场景 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|------|-------|
| POST-01 | 查看板块列表 | 有板块数据 | 访问首页→板块导航 | 显示所有分区+子板块树形结构 | P0 |
| POST-02 | 进入板块看帖 | 板块有帖子 | 点击板块→帖子列表 | 显示帖子，按时间/热度排序 | P0 |
| POST-03 | 帖子分页 | 帖子>1页 | 翻到第2页 | 加载第2页数据 | P1 |
| POST-04 | 空板块 | 板块无帖子 | 进入该板块 | 显示"暂无帖子"空状态 | P1 |
| POST-05 | 查看帖子详情 | 帖子存在 | 点击标题 | 显示完整内容/附件/投票/标签 | P0 |
| POST-06 | 含投票的详情 | 帖子含投票 | 进入详情 | 显示投票选项和当前结果 | P1 |
| POST-07 | 创建普通帖 | 已登录 | 选板块→输入标题+内容→选normal→提交 | 201发布成功 | P0 |
| POST-08 | 创建投票帖 | 已登录 | 选poll→标题+内容+≥2选项→提交 | 发布成功，详情显示投票 | P1 |
| POST-09 | 创建长文帖 | 已登录 | 选longtext→长内容+封面图→提交 | 发布成功，详情显示封面图 | P1 |
| POST-10 | 创建实时帖 | 已登录 | 选realtime→勾选is_live→提交 | 发布成功，标记实时 | P2 |
| POST-11 | 未登录发帖 | 未登录 | 访问发帖页 | 重定向到登录 | P0 |
| POST-12 | 含敏感词被拦截 | 有BLOCK级敏感词 | 输入含"禁发词" | 拦截提示 | P1 |
| POST-13 | 含敏感词需审核 | 有REVIEW级敏感词 | 输入含"审核词" | 帖子进入审核 | P1 |
| POST-14 | 投票选项<2个 | 已登录 | poll只加1项→提交 | 422校验错误 | P1 |
| POST-15 | 编辑帖子 | 本人帖子 | 修改→保存 | 内容更新 | P1 |
| POST-16 | 非作者编辑 | 他人帖子 | 尝试编辑 | 403无权限 | P1 |
| POST-17 | 删除帖子 | 本人帖子 | 删除→确认 | 帖子被删除 | P1 |
```

### 3.3 评论与互动

```
根据以下交互场景，生成完整的功能测试用例表。

【场景描述】
用户在帖子详情页阅读评论，发表评论，回复他人（楼中楼），
给帖子/评论点赞/取消，收藏/取消收藏，转发帖子。

【用例清单】

| 编号 | 场景 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|------|-------|
| CMT-01 | 查看评论 | 帖子有评论 | 进入帖子→评论区 | 评论列表，楼中楼缩进 | P0 |
| CMT-02 | 无评论帖子 | 帖子无评论 | 查看评论区 | "暂无评论"空状态 | P0 |
| CMT-03 | 发表一级评论 | 已登录 | 输入→提交 | 201，评论显示在列表 | P0 |
| CMT-04 | 楼中楼回复 | 有评论 | 点回复→输入→提交 | 显示在父评论下缩进 | P1 |
| CMT-05 | 回复指定用户 | 有评论 | 点某人回复→输入 | 被回复者收到通知 | P1 |
| CMT-06 | 评论含敏感词 | 有REVIEW词 | 输入含敏感词 | 评论进入审核 | P1 |
| CMT-07 | 空评论 | 已登录 | 不输入直接提交 | 422校验错误 | P1 |
| CMT-08 | 删除评论 | 本人评论 | 删除→确认 | 评论消失 | P1 |
| CMT-09 | 非作者删除 | 他人评论 | 尝试删除 | 403无权限 | P1 |
| CMT-10 | 帖子点赞 | 已登录 | 点帖子点赞按钮 | 点赞+1，按钮高亮 | P0 |
| CMT-11 | 取消点赞 | 已点赞 | 再次点击 | 点赞-1，取消高亮 | P1 |
| CMT-12 | 评论点赞 | 已登录 | 点评论点赞 | 评论点赞+1 | P1 |
| CMT-13 | 收藏帖子 | 已登录 | 点收藏 | 收藏成功 | P1 |
| CMT-14 | 取消收藏 | 已收藏 | 再点收藏 | 取消收藏 | P1 |
| CMT-15 | 查看收藏 | 有收藏 | 进"我的收藏" | 显示收藏的帖子 | P1 |
| CMT-16 | 无收藏查看 | 无收藏 | 进"我的收藏" | "暂无收藏" | P1 |
| CMT-17 | 转发帖子 | 已登录 | 点转发→输入语/直接转 | 转发+1 | P2 |
| CMT-18 | 未登录点互动 | 未登录 | 点点赞/收藏 | 弹出登录提示 | P0 |
```

### 3.4 社交关注

```
根据以下交互场景，生成完整的功能测试用例表。

【场景描述】
用户搜索其他用户，进入对方主页查看资料，点击关注/取消关注，
查看自己的粉丝/关注列表，设置星标用户，配置隐私后验证效果。

【用例清单】

| 编号 | 场景 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|------|-------|
| SOC-01 | 查看用户主页 | 用户存在 | 点用户名→主页 | 显示头像/昵称/简介/帖子 | P0 |
| SOC-02 | 关注用户 | 已登录 | 点关注按钮 | 关注成功，变"已关注" | P0 |
| SOC-03 | 取消关注 | 已关注 | 再次点击 | 取消关注 | P1 |
| SOC-04 | 关注自己 | 已登录 | 自己主页点关注 | 不可操作 | P1 |
| SOC-05 | 查看关注列表 | 有关注 | 个人中心→关注 | 显示关注的人 | P1 |
| SOC-06 | 查看粉丝列表 | 有粉丝 | 个人中心→粉丝 | 显示粉丝 | P1 |
| SOC-07 | 空关注/粉丝 | 无 | 查看 | 空状态提示 | P1 |
| SOC-08 | 查看他人关注/粉丝 | 公开用户 | 对方主页→列表 | 显示列表 | P1 |
| SOC-09 | 设星标 | 已关注 | 关注列表→设星标 | 置顶显示 | P2 |
| SOC-10 | 隐私-仅粉丝可见 | 设置followers_only | 非粉丝查看 | 部分信息隐藏 | P1 |
| SOC-11 | 隐私-私密 | 设置private | 非粉丝查看 | 仅昵称头像 | P1 |
| SOC-12 | 隐私-粉丝可见通过 | followers_only | 粉丝查看 | 完整信息 | P1 |
| SOC-13 | 不存在的用户 | - | 访问/users/99999 | 404 | P1 |
```

### 3.5 群组社区

```
根据以下交互场景，生成完整的功能测试用例表。

【场景描述】
用户创建投资群组（公开/私密），其他用户搜索加入，在群内发帖，
群主审核成员申请，管理成员，最后解散群组。

【用例清单】

| 编号 | 场景 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|------|-------|
| GRP-01 | 创建公开群 | 已登录 | 填名称/描述→设公开→提交 | 群创建成功 | P0 |
| GRP-02 | 创建私密审批群 | 已登录 | 设私密+需审批→提交 | 创建成功 | P1 |
| GRP-03 | 未登录创建 | 未登录 | 访问创建页 | 重定向登录 | P0 |
| GRP-04 | 搜索群组 | 群存在 | 搜名称 | 搜索结果含群 | P0 |
| GRP-05 | 加入公开群 | 未加入 | 点加入 | 直接加入 | P0 |
| GRP-06 | 申请私密群 | 未加入 | 点加入 | 状态"待审核" | P1 |
| GRP-07 | 重复加入 | 已是成员 | 再点加入 | 提示已是成员 | P1 |
| GRP-08 | 审核通过 | 有申请 | 审核→通过 | 成员可看群 | P1 |
| GRP-09 | 审核拒绝 | 有申请 | 审核→拒绝+原因 | 通知申请人 | P1 |
| GRP-10 | 群内发帖 | 已加入 | 群内点发帖→输入→提交 | 帖仅群内可见 | P1 |
| GRP-11 | 非成员看群帖 | 未加入 | 访问群内帖子 | 403 | P1 |
| GRP-12 | 退出群组 | 已加入 | 退出→确认 | 无法看群内容 | P1 |
| GRP-13 | 移出成员 | 群主 | 成员管理→移出 | 成员被移除 | P1 |
| GRP-14 | 编辑群信息 | 群主 | 编辑→保存 | 群信息更新 | P1 |
| GRP-15 | 解散群组 | 群主 | 解散→确认 | 成员收到通知，群消失 | P1 |
| GRP-16 | 非群主解散 | 普通成员 | 尝试解散 | 403 | P1 |
```

### 3.6 私信与消息

```
【场景描述】用户给已关注的好友发私信，在群组中发群聊消息，查看消息列表。

| 编号 | 场景 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|------|-------|
| MSG-01 | 发送私信 | 有关注 | 用户主页→发私信→输入→发送 | 私信发出 | P0 |
| MSG-02 | 给未关注者发 | 对方限制 | 发私信 | 403被拦截 | P1 |
| MSG-03 | 群聊发消息 | 已加群 | 群聊→输入→发送 | 群消息显示 | P1 |
| MSG-04 | 消息未读标记 | 有新私信 | 看消息列表 | 显示未读红点 | P1 |
| MSG-05 | 消息已读 | 有未读 | 打开对话 | 未读消除 | P1 |
| MSG-06 | 空消息列表 | 无消息 | 进消息页 | "暂无消息" | P1 |
```

### 3.7 搜索与发现

```
【场景描述】用户输入关键词搜索，查看联想建议，切换结果类别，浏览热榜和推荐。

| 编号 | 场景 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|------|-------|
| SRH-01 | 搜索联想 | 有数据 | 输入"000" | 显示匹配联想 | P0 |
| SRH-02 | 搜索帖子 | 有帖子 | 输入关键词→搜索 | 匹配帖子列表 | P0 |
| SRH-03 | 搜索用户 | 有用户 | 切用户tab | 匹配用户 | P1 |
| SRH-04 | 搜索群组 | 有群组 | 切群组tab | 匹配群组 | P1 |
| SRH-05 | 搜索股票 | 内置列表 | 切股票tab | 匹配股票 | P1 |
| SRH-06 | 空结果 | 无匹配 | 搜不存在的词 | "没有找到相关结果" | P1 |
| SRH-07 | 查看热榜 | 有帖子 | 切热榜Tab | 按热度排序 | P0 |
| SRH-08 | 热榜时间筛选 | 有数据 | 切换日/周/月 | 数据变化 | P1 |
| SRH-09 | 个性化Feed | 有关注 | 首页Feed | 关注人+推荐混合 | P0 |
| SRH-10 | 个性化推荐 | 有偏好 | 访问推荐 | 匹配偏好内容 | P1 |
```

### 3.8 通知

```
【场景描述】用户收到多种通知（被点赞/新粉丝/@提及），查看通知列表，标记已读。

| 编号 | 场景 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|------|-------|
| NTF-01 | 收到点赞通知 | 帖子被点赞 | 查看通知 | "XXX赞了你的帖子" | P1 |
| NTF-02 | 新粉丝通知 | 被关注 | 查看通知 | "XXX关注了你" | P1 |
| NTF-03 | @提及通知 | 被@ | 查看通知 | "XXX在帖子中@了你" | P1 |
| NTF-04 | 系统通知 | 管理员发送 | 查看通知 | 系统通知内容 | P1 |
| NTF-05 | 按类型筛选 | 有多类型 | 选筛选条件 | 只显示该类型 | P1 |
| NTF-06 | 未读计数 | 有未读 | 看铃铛 | 显示未读数 | P0 |
| NTF-07 | 标记单条已读 | 有未读 | 点单条 | 变为已读 | P1 |
| NTF-08 | 标记全部已读 | 有未读 | 点"全部已读" | 全部已读 | P1 |
| NTF-09 | 通知跳转 | 有点赞通知 | 点通知 | 跳到对应帖子 | P1 |
```

### 3.9 行情浏览

```
【场景描述】用户查看实时指数行情，进入K线详情，切换周期。

| 编号 | 场景 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|------|-------|
| MKT-01 | 实时行情 | 数据源正常 | 访问首页 | 指数行情卡片 | P0 |
| MKT-02 | 查看K线 | 有数据 | 点指数→K线页 | 显示K线图表 | P1 |
| MKT-03 | 切换K线周期 | K线显示 | 切换日/周/月 | 图表更新 | P1 |
| MKT-04 | 数据不可用 | 源全不可用 | 访问行情 | "暂不可用"提示 | P2 |
```

### 3.10 积分与等级

```
【场景描述】用户每日登录签到，发帖评论获取积分，查看积分和等级。

| 编号 | 场景 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|------|-------|
| PTS-01 | 每日签到加分 | 当日首次登录 | 登录 | 积分+1 | P1 |
| PTS-02 | 同日不重复加 | 当日已登录 | 重新登录 | 积分不变 | P1 |
| PTS-03 | 发帖加分 | 已登录 | 创建帖子 | 积分+5 | P1 |
| PTS-04 | 评论加分 | 已登录 | 发表评论 | 积分+2 | P1 |
| PTS-05 | 收点赞加分 | 被点赞 | 别人点赞 | 积分+1 | P2 |
| PTS-06 | 帖子被转发 | 被转发 | 别人转发 | 积分+3 | P2 |
| PTS-07 | 删帖扣分 | 已发帖 | 删除帖子 | 积分-5 | P2 |
| PTS-08 | 查看积分历史 | 有记录 | 进积分页面 | 积分变动明细 | P1 |
| PTS-09 | 查看等级 | 有积分 | 个人主页 | 等级+升级进度 | P1 |
```

### 3.11 认证与风险评估

```
【场景描述】用户申请实名认证/专业认证，完成风险评估问卷。

| 编号 | 场景 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|------|-------|
| CRT-01 | 提交实名认证 | 未认证 | 填姓名+身份证+传照片→提交 | 待审核 | P1 |
| CRT-02 | 重复提交实名 | 已提交 | 再次提交 | 提示已提交 | P1 |
| CRT-03 | 提交专业认证 | 已实名 | 传资格证明→提交 | 待审核 | P1 |
| CRT-04 | 未实名提专业认证 | 未实名 | 直接提交 | 提示先实名 | P1 |
| CRT-05 | 完成风险评估 | 已登录 | 答10题→提交 | 显示风险等级 | P1 |
| CRT-06 | 查看评估历史 | 有评估 | 进评估历史 | 历次结果 | P2 |
```

### 3.12 个人设置与隐私

```
【场景描述】用户修改个人资料，配置隐私选项。

| 编号 | 场景 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|------|-------|
| SET-01 | 修改昵称 | 已登录 | 设置→昵称→修改→保存 | 昵称更新 | P0 |
| SET-02 | 修改简介 | 已登录 | 设置→简介→修改→保存 | 简介更新 | P1 |
| SET-03 | 更换头像 | 已登录 | 上传新图→保存 | 头像更新 | P1 |
| SET-04 | 设资料为公开 | 已登录 | 隐私→public→保存 | 所有人可见 | P1 |
| SET-05 | 设仅粉丝可见 | 已登录 | privacy=followers_only | 非粉丝受限 | P1 |
| SET-06 | 设私密 | 已登录 | privacy=private | 仅昵称头像 | P1 |
| SET-07 | 关闭消息权限 | 已登录 | message_permission=none | 不可私信 | P2 |
```

### 3.13 管理后台

```
【场景描述】管理员登录后台，审核内容、管理用户、管理板块、管理敏感词。

| 编号 | 场景 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|------|-------|
| ADM-01 | 管理员登录 | 有管理员账号 | 管理员登录→进后台 | 进入后台界面 | P0 |
| ADM-02 | 非管理员访问 | 普通用户 | 访问/admin | 重定向或403 | P0 |
| ADM-03 | 仪表盘总览 | 已登录 | 进仪表盘 | DAU/帖子数/用户数/趋势 | P0 |
| ADM-04 | 审核队列 | 有待审 | 进审核队列 | 待审核列表 | P0 |
| ADM-05 | 审核通过 | 有审核项 | 点通过 | 内容发布，通知作者 | P0 |
| ADM-06 | 审核拒绝 | 有审核项 | 点拒绝+填意见 | 内容下架，通知作者 | P1 |
| ADM-07 | 用户列表 | 有用户 | 进用户管理 | 用户列表 | P0 |
| ADM-08 | 搜索用户 | 有用户 | 输入昵称搜索 | 匹配用户 | P0 |
| ADM-09 | 封禁用户 | 用户正常 | 封禁+时长+原因 | 用户无法登录发帖 | P0 |
| ADM-10 | 解封用户 | 已封禁 | 解封 | 用户恢复 | P1 |
| ADM-11 | 封禁后验证 | 刚封禁 | 被封账号尝试登录+发帖 | 登录提示被封，发帖403 | P0 |
| ADM-12 | 板块列表 | 有数据 | 进板块管理 | 分区+子板块树形 | P0 |
| ADM-13 | 新增分区 | 管理员 | 新建→名称/描述/排序→提交 | 前端显示新分区 | P0 |
| ADM-14 | 新增子板块 | 有分区 | 分区下→新建子板块→提交 | 显示在分区下 | P1 |
| ADM-15 | 编辑板块 | 板块存在 | 修改名称/排序/状态→保存 | 更新生效 | P1 |
| ADM-16 | 停用板块 | 板块存在 | 状态→停用 | 前端不显示 | P1 |
| ADM-17 | 删除板块 | 板块存在 | 删除→确认 | 子板块提升为顶级 | P1 |
| ADM-18 | 新增敏感词 | 管理员 | 输入词+级别+分类→提交 | 添加成功 | P0 |
| ADM-19 | 敏感词BLOCK测试 | 已加BLOCK词 | 发帖含该词 | 帖子被拦截 | P0 |
| ADM-20 | 敏感词REVIEW测试 | 已加REVIEW词 | 发帖含该词 | 帖子进审核 | P1 |
| ADM-21 | 停用敏感词 | 词存在 | 停用→保存 | 不再生效 | P1 |
| ADM-22 | 认证审核 | 有待审 | 进认证审核→通过/拒绝 | 用户认证状态更新 | P1 |
| ADM-23 | 操作日志 | 有操作 | 进操作日志 | 时间线显示操作 | P1 |
| ADM-24 | 热门话题分析 | 有帖子 | 进热门话题 | 排行+趋势 | P2 |
| ADM-25 | 参与度报告 | 有活动数据 | 进参与度 | 互动率/活跃度 | P2 |
```

### 3.14 安全与权限边界

```
生成跨模块的安全与权限边界测试用例。

| 编号 | 场景 | 步骤 | 预期 | 优先级 |
|------|------|------|------|-------|
| SEC-01 | 未登录访问需登录接口 | 直接请求POST /api/posts | 401 | P0 |
| SEC-02 | 越权删他人帖子 | A的Token删B的帖子 | 403 | P0 |
| SEC-03 | 越权编辑他人帖子 | A的Token编B的帖子 | 403 | P0 |
| SEC-04 | 普通用户访问管理接口 | 普通Token请求/admin/* | 403 | P0 |
| SEC-05 | Token伪造 | 随机字符串作Token | 401 | P0 |
| SEC-06 | Token过期 | 过期Token请求 | 401 | P1 |
| SEC-07 | SQL注入尝试 | 搜索"'OR 1=1--" | 不返回敏感数据 | P1 |
| SEC-08 | XSS尝试 | 帖子内容输入"<script>alert('xss')</script>" | 脚本不执行 | P1 |
| SEC-09 | 越权看私密群 | 未加入直接访问 | 403 | P1 |
| SEC-10 | 越权看私密用户资料 | private用户非粉丝查看 | 仅昵称头像 | P1 |
| SEC-11 | 越权解散群组 | 普通成员尝试 | 403 | P1 |
| SEC-12 | 超长字段 | 输入超长昵称/标题 | 422或截断 | P1 |
```

### 3.15 端到端综合流程

```
生成覆盖多模块联动的端到端测试用例。

| 编号 | 场景 | 步骤 | 预期 | 优先级 |
|------|------|------|------|-------|
| E2E-01 | 完整用户旅程 | 注册→登录→浏览板块→看帖→评论→点赞→收藏→关注→退出→重登 | 每步正常，数据一致 | P0 |
| E2E-02 | 发帖到审核到上架 | 普通用户发帖(含敏感词)→管理员审核通过→对所有人可见 | 审核流程完整 | P1 |
| E2E-03 | 群组全流程 | A创建私密群→B申请→A审批→B群内发帖→A解散 | 每步权限正确 | P1 |
| E2E-04 | 积分累积链路 | 注册→登录(签到)→发帖→评论→被点赞→查看积分 | 积分正确累积 | P2 |
| E2E-05 | 通知触发全链路 | A关注B→B发帖→A点赞→A评论→B查看通知→看到所有 | 每步触发对应通知 | P1 |
| E2E-06 | 搜索到互动链路 | 搜索→看帖→评论→点赞→收藏→查收藏列表 | 完整链路正常 | P1 |
| E2E-07 | 封禁到解封 | 管理员封禁→用户无法登录→解封→用户恢复 | 封禁/解封生效 | P0 |
```

---

## 附录：项目模块与测试对照表

| 业务模块 | 对应API文件 | 对应Service | 单元测试 | 接口测试 | 功能测试 |
|---------|------------|-------------|---------|---------|---------|
| 用户注册登录 | auth.py | user_service.py | 1.1,1.2,1.3,1.4,1.11 | 2.1 | 3.1 |
| 帖子内容 | posts.py | duplicate_content, quality | 1.5,1.9 | 2.2 | 3.2 |
| 评论/点赞/收藏/转发 | interactions.py | - | - | 2.3 | 3.3 |
| 社交关系 | social_users.py | - | - | 2.4 | 3.4 |
| 群组社区 | community.py | - | - | 2.5 | 3.5 |
| 私信消息 | community.py | - | - | 2.5 | 3.6 |
| 发现/搜索/推荐 | discovery.py | - | - | 2.6 | 3.7 |
| 行情数据 | market.py | (内置) | 1.12 | 2.7 | 3.9 |
| 通知 | notifications.py | mention_service | 1.10 | 2.8 | 3.8 |
| 管理后台 | admin.py | - | - | 2.9 | 3.13 |
| 积分等级 | - | points_service | 1.6 | - | 3.10 |
| 敏感词检测 | - | sensitive_word_service | 1.7 | - | 3.13 |
| 合规检测 | - | compliance_service | 1.8 | - | 3.13 |
| 认证评估 | auth.py | user_service | - | 2.1 | 3.11 |
| 个人设置/隐私 | auth.py | user_service | - | 2.1 | 3.12 |
| 安全边界 | 全部 | 全部 | 1.11 | 2.1-2.9 | 3.14 |
| 端到端综合 | 全部 | 全部 | - | - | 3.15 |
